#include <stdio.h>
int main() {
    int x, y;
    printf("Enter two integers: ");
    scanf("%d %d", &x, &y);
    int result = x ^ y;
    printf("Bitwise XOR of %d and %d is: %d\n", x, y, result);
    return 0;
}
