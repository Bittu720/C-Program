#include <stdio.h>
void printToN(int n) {
    if (n == 0)
        return;
    printf("%d ", n); 
printToN(n - 1); 
}

int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    printToN(n);

    return 0;
}
